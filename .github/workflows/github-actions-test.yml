name: GitHub Actions NPM CI Test

on: [push]

jobs:
   build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo code
        uses: actions/checkout@v30
      - uses: actions/setup-node@v3
        with:
          node-version: 16
        - run: npm ci
        - run: npm test